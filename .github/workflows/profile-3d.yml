name: GitHub-Profile-3D-Contrib

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-3d-contrib
    steps:
      - uses: actions/checkout@v3
      - uses: yoshi389111/github-profile-3d-contrib@0.7.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: zara-shahid
          SETTING_JSON: |
            {
              "githubUserName": "zara-shahid",
              "type": "NORMAL",
              "theme": "radical"
            }
      - name: Commit & Push
        run: |
          git config user.email "zarashahid444@gmail.com"
          git config user.name "zara-shahid"
          git add -A .
          git commit -m "generate 3d contrib" || exit 0
          git push
